# GENV
#### A tool to auto-generate `env.d.ts`
## Installation
#### `.zshrc.js` or `.bashrc.js`
```bash
export PATH="$PATH:/.../genv/"
```
## Running
After installation run with:
#### uses .env at working directory
```bash
genv.sh
```
#### uses .env at supplied path
```bash
genv.sh ./path/.env
```